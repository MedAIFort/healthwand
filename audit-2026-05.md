# Repository Audit - 2026-05-13

## Cargo.toml (phi-detector)

```healthwand/phi-detector/Cargo.toml#L1-200
[package]
name = "phi-detector"
version = "0.1.0"
edition = "2024"
authors = ["Kresna Sucandra <https://github.com/SHA888>"]
license = "MIT"

[lib]
name = "phi_detector"
path = "src/lib.rs"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
tempfile = "3.10"
regex = "1.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"
log = "0.4"
env_logger = "0.10"
thiserror = "1.0"
```

## Cargo.lock (phi-detector)

```healthwand/phi-detector/Cargo.lock#L1-200
# Cargo.lock content omitted for brevity. Present in repository.
```

## CI Workflows

No CI workflows found under `.github/workflows/`.

## Top-level Directory Tree (3 levels)

```
healthwand/
├─ .git/
├─ docs/
├─ phi-detector/
│  ├─ config/
│  ├─ docs/
│  ├─ src/
│  └─ tests/
├─ scripts/
└─ .github/
   └─ instructions/
```

## Last Commit Dates per Major Path

- Root (including top-level files): 2026-05-13 18:31:05 +0800
- `phi-detector/`: 2026-05-13 18:31:05 +0800
- `docs/`: 2026-05-13 18:31:05 +0800
- `scripts/`: 2026-05-13 18:31:05 +0800

_Generated automatically._

### Python Minimum Version

- Decided Python floor for NLP companion: **Python 3.11+** (confirmed by Python Developer's Guide).

### Minimum Supported Rust Version (MSRV)

- Determined MSRV for `phi-detector` crate: **Rust 1.87.0** (via `cargo msrv find`).

### Crates.io Name Availability

- Crate name **`healthwand`** is **available** on crates.io (no existing crate).

### CLI Flag Compatibility Verification

Verified on `sample.txt` (2026-05-13):

| Flag               | Result                                                                                                   |
| ------------------ | -------------------------------------------------------------------------------------------------------- |
| Default (no flags) | JSON output with 4 detections, `redacted_count: 0`                                                       |
| `--output text`    | Human‑readable text summary with detection list                                                          |
| `--redact`         | JSON output with `redacted_text` fields populated (e.g., `XXX-XX-XXXX`, `XXXXXXXX`), `redacted_count: 4` |
| `-v`               | Info log line: `[INFO healthwand] Starting PHI detection pipeline`                                       |
| `-vv`              | Debug level set, flag accepted                                                                           |

All flags behave exactly as before the rename.

### phi-detector Build and Test Results

- **Existing crates.io `phi-detector` crate:** Implements a _Phi Accrual Failure Detector_ (not PHI detection). It has reverse dependencies such as `lol-core` (≈39k stars), `lolraft` (≈4k stars), and `sorock` (≈2k stars). This indicates active usage, reinforcing the need to rename our crate to avoid conflict with this unrelated project.

- **Third‑party references:** No external mentions of `phi-detector` found in other repositories, blogs, or social media (searches of GitHub, Google, and common platforms returned no results).

- **Note:** There is an existing, unmaintained crate `phi-detector` on crates.io (not ours). To avoid confusion, we will rename our crate.

- **cargo build --release**: succeeded
- **cargo test**: all 28 tests passed
- **cargo clippy**: no warnings (treated as errors)
